Faça uma copia do parameters-template.sh com o nome parameters.sh

Execute o deploy.sh

Apos a execução com sucesso descomente o `export USER_POOL_ID=` e adicione o valor gerado apos ter seguido o tutorial de configuracao do IAM Identity Center e faça um commit e um push para o repositorio remoto para um novo deploy do amplify